---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "unifi_setting_ether_lighting Resource - terraform-provider-unifi"
subcategory: ""
description: |-
  Manages the site-level Etherlighting palette — the colors that switches with per-port LEDs (USW Pro Max line) use when a device's ether_lighting block selects a scheme. network_overrides assigns a color per network/VLAN (used by mode = "network"); speed_overrides assigns a color per link-speed class (used by mode = "speed"). Overrides take precedence over the controller's built-in default palette; networks or speeds without an override keep their default color. NOTE: the controller silently drops any override whose color equals that network's built-in default — declare only colors that actually differ from the defaults, or the entry will not round-trip.
---

# unifi_setting_ether_lighting (Resource)

Manages the site-level Etherlighting palette — the colors that switches with per-port LEDs (USW Pro Max line) use when a device's `ether_lighting` block selects a scheme. `network_overrides` assigns a color per network/VLAN (used by `mode = "network"`); `speed_overrides` assigns a color per link-speed class (used by `mode = "speed"`). Overrides take precedence over the controller's built-in default palette; networks or speeds without an override keep their default color. NOTE: the controller silently drops any override whose color equals that network's built-in default — declare only colors that actually differ from the defaults, or the entry will not round-trip.



<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `network_overrides` (Attributes Set) Per-network LED colors, used when a device's Etherlighting `mode` is `network`. (see [below for nested schema](#nestedatt--network_overrides))
- `site` (String) The name of the UniFi site where this resource should be applied. If not specified, the default site will be used.
- `speed_overrides` (Attributes Set) Per-link-speed LED colors, used when a device's Etherlighting `mode` is `speed`. (see [below for nested schema](#nestedatt--speed_overrides))

### Read-Only

- `id` (String) The unique identifier of this resource.

<a id="nestedatt--network_overrides"></a>
### Nested Schema for `network_overrides`

Required:

- `color_hex` (String) LED color as a 6-digit RGB hex string without `#` (e.g. `ff6c14`).
- `network_id` (String) ID of the network/VLAN this color applies to (e.g. `unifi_network.iot.id`).


<a id="nestedatt--speed_overrides"></a>
### Nested Schema for `speed_overrides`

Required:

- `color_hex` (String) LED color as a 6-digit RGB hex string without `#` (e.g. `ffc107`).
- `speed` (String) Link-speed class this color applies to.
